# Battery watcher configuration
# Copy to /etc/battery-watcher.conf

# Notification command (message is appended)
# For ntfy.sh via kc-notify:
#BATTERY_NOTIFY_CMD=/usr/local/bin/kc-notify alert

# For desktop notifications:
#BATTERY_NOTIFY_CMD=/usr/bin/notify-send "Battery Alert"
